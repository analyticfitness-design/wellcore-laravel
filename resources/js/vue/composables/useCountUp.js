/**
 * useCountUp — anima un número desde 0 hasta su valor final.
 * Stub FASE 1; FASE 2 implementa la animación real con requestAnimationFrame.
 *
 * @param {Ref<HTMLElement|null>} elRef - elemento DOM que muestra el número
 * @param {Function} getValue - getter reactivo del valor final
 * @param {Object} options - { duration: ms }
 */
export function useCountUp(_elRef, _getValue, _options = {}) {
    // FASE 2: implementar animación con requestAnimationFrame
}
